import{leer archivo } from "./02-leer-archivo";
import { escribirtArchivo } from "./03-escribir.archivos";

function main(){
    const textoLeido = LeerArchivo();
    escribirtArchivo(nuevoContenido);

    const textoLeido = leerArchivo);
    console.log(textoLeido);
    

 
}